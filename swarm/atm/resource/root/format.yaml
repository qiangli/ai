###
pack: "{{.pack}}"
agents:
  - name: "{{.name}}"
    display: "{{.display}}"
    description: "{{.description}}"
    environment: {}
    parameters: {}
    arguments: {}
    #
    adapter: {}
    #
    model: "{{.set_level}}"
    context: |
      {{.context}}
    message: |
      {{.query}}
    instruction: |
      {{.prompt}}
    functions: []

###
kit: "{{.kit}}"
tools:
  - name: "{{.name}}"
    description: "{{.description}}"
    type: "{{.type}}"
    parameters: {}
    arguments: {}
    body: {}

###
set: "{{.set}}"
models:
  {{.level}}:
    model: "{{.model}}"
    provider: "{{.provider}}"
    base_url: "{{.base_url}}"
    api_key: "{{.api_key}}"
###
