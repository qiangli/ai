###
kit: <tool kit name>

# supported tool type:
# web: (builtin) PR required
# mcp: model context protocol
# agent: this is the agent you created
# func: inline function body (python)
type: "web | mcp | agent | func"
# Add an entry "<key-name>" with your service API Key at:
# https://ai.dhnt.io/settings/api-key
api_key: "<key-name>"

# mcp
connector:
  base_url: "<service endpoint>"

# func | web
tools:
  - name: ""
    description: ""
    parameters: {}
    body:
###