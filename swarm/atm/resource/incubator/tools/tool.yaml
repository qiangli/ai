###
kit: <tool kit name>

# func: inline function body
# agent: use agent as tool
type: "func | agent"

# func | web
tools:
  - name: "function name"
    description: "description"
    parameters:
      type: "object"
      properties:
        path:
          type: "string | integer | boolean ..."
          description: ""
        # more property definitions ...
    body: |
      #!golang

###