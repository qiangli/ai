###
# max_time: 100
# timeout: "@timeout"
# log_level: info
# logger: "@logger"
agents:
  - name: "new"
    display: "🤖 Creator Agent"
    description: |
      The *new* creator agent is an intelligent and versatile digital assistant
      designed to handle diverse tasks by leveraging a dynamic system prompt
      based on user's current input. In addition to the dynamic prompt, it also
      determines the best model based on user input, selects/summarizes the
      historical messages as appropriate to the current user input. What is more,
      it dynamically determines and runs tools if they are helpful for answering
      users' queries as well.
    # message: |
    #   @query
    instruction:
      content: |
       #! 
       {{ai @ask}}
    context: |
      #!
    model: |
      #!
      
    functions:
      - "@tool"
      # - "ai:context_get_messages"
      # - "ai:context_set_messages"
      # - "ai:get_envs"
      # - "ai:set_envs"
      # - "ai:unset_envs"
      # - "ai:agent_get_prompt"
      # - "ai:agent_set_prompt"
      # - "ai:agent_get_query"
      # - "ai:agent_set_query"
      # - "ai:agent_get_model"
      # - "ai:agent_set_model"
      # - "ai:agent_get_tools"
      # - "ai:agent_set_tools"
###