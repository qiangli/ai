{
	// Use IntelliSense to learn about possible attributes.
	// Hover to view descriptions of existing attributes.
	// For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Agent",
			"type": "go",
			"request": "launch",
			"mode": "auto",
			"program": "${workspaceFolder}/cmd/",
			"env": {
				// "OPENAI_API_KEY": "${env:OPENAI_API_KEY}",
				// "GEMINI_API_KEY": "${env:GEMINI_API_KEY}",
				// "ANTHROPIC_API_KEY": "${env:ANTHROPIC_API_KEY}",
				// "BRAVE_API_KEY": "${env:BRAVE_API_KEY}",
				// "GOOGLE_API_KEY": "${env:GOOGLE_API_KEY}",
				// "DHNT_API_KEY": "${env:DHNT_API_KEY}",
				"AI_DEBUG": "true",
			},
			"envFile": "${workspaceFolder}/.env",
			"args": [
				// "/help",
				// "/sh:bash",
				// "/bin/ls",
				// "-al",
				// "/sh:bash", "ls",
				// "agents",
				// "/agent:joker",
				// "@agent/agent",
				// "@ask",
				// "@eval",
				"@git",
				"message",
				"call git/short to generate a git commit message",
				"-",
				// "@iconvert",
				// "@news",
				// "@video",
				// "@think",
				// "@timer",
				// "--message", "set a timer for 10 minutes",
				// "@chat",
				// "@gptr",
				// "@gpte",
				// "@aider",
				// "@research",
				// "@tool",
				// "--arg", "kit=agent",
				// "--arg", "name=memory",
				// "--workspace",
				// "/tmp/test/aider",
				// "--message",
				// "what are the tools you are provided with",
				// "write a tool tha runs a simple python hello world and save it in the workspace",
				// "--message", 
				// "read jokes.md from the memory",
				// "tell me a joke",
				// "tell me a joke. show me the query and prompt used for the llm request by calling call_llm and other tools",
				// "@gptr/crawl",
				// "@agent",
				// "--message",
				// "what is the weather today in dublin ca",
				// "yes",
				// "the weather in dublin ca",
				// "@cron",
				// "--message", "schedule a chat on climate change at 3:00 pm tomorrow pdt",
				// "--no-stdin=true",
				// "-",
				// "--message",
				// "write short git message",
				// "how to design a multi agent system",
				// "give me a summary report of the state of the arts on vibe coding using LLM",
				// "generate a random password",
				// "what is the weather in dublin ca today",
				// "use sora from openai to make a video of a german shepard puppy running in bamboo forest. save the video in /tmp as gsd-puppy.mp4",
				// "--verbose",
				// "--max-time",
				// "900",
				// "--max-history",
				// "100",
				// "--max-span",
				// "14400"
				// "about",
				// "space technology"
				// "tell", "a", "joke"
			],
			"showLog": false,
			"stopOnEntry": false
		},
		// {
		// 	"name": "Dry Run",
		// 	"type": "go",
		// 	"request": "launch",
		// 	"mode": "auto",
		// 	"program": "${workspaceFolder}/cmd/",
		// 	"env": {
		// 		"AI_MODEL": "gpt-4o-mini",
		// 		"AI_API_KEY": "${env:OPENAI_API_KEY}",
		// 		"AI_BASE_URL": "http://localhost:8000/v1",
		// 		"AI_DEBUG": "true",
		// 	},
		// 	"args": [
		// 		"--dry-run=true",
		// 		"--dry-run-content",
		// 		"test",
		// 		"--verbose",
		// 		"/",
		// 		"--message",
		// 		"what is coooking"
		// 	],
		// 	"showLog": false,
		// 	"stopOnEntry": false,
		// },
	]
}