###

agents:
  - name: "draw"
    display: "🐝 Draw"
    state: "transfer"
    description: "Generate images based on user input, providing visual representations of text-based descriptions."
    model: Image
    instruction:
      content: "file:image_param_system_role.md"
    # advices:
    #   before: "image_params"

###
